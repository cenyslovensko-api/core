[package]
name = "cenyslovensko_web_client"
version = "0.1.6"
edition = "2024"
description = "Configurable HTTP web client for the CenySlovensko API (base URI, headers, timeouts, proxy, log level)."
license.workspace = true
repository.workspace = true

[dependencies]
reqwest = { workspace = true }
derive_more = { workspace = true, features = ["display", "error"] }
miette = { workspace = true }
